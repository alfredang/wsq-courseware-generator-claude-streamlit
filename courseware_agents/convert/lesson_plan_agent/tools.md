# Convert Lesson Plan Agent — Tools

## AI Model
- **Claude Sonnet** via `run_agent_json()` (Claude Agent SDK)

## Tools Used
- None (text-only extraction, no file system tools needed)

## Dependencies
- `python-docx` — Read source DOCX files
- `docxtpl` — Fill standardised LP DOCX template with extracted data
