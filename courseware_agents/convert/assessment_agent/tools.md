# Convert Assessment Agent — Tools

## AI Model
- **Claude Sonnet** via `run_agent_json()` (Claude Agent SDK)

## Tools Used
- None (text-only extraction, no file system tools needed)

## Dependencies
- `python-docx` — Read source DOCX files
- `generate_assessment.assessment_generation._build_assessment_doc()` — Rebuild in WSQ format
